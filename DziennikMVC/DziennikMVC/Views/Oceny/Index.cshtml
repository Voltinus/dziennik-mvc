@model IEnumerable<DziennikMVC.Models.Ocena>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ocena)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.opis_oceny)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.czy_koncowa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.data)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ocena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.opis_oceny)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.czy_koncowa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.data)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OcenaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OcenaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OcenaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
